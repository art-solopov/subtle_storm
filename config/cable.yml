sc_default: &sc_default
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  polling_interval: 0.1.seconds
  message_retention: 1.day

development:
  <<: *sc_default

test:
  adapter: test

production:
  <<: *sc_default
